Let the cloud server use 4GB of RAM!
org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G

android.useAndroidX=true
# Tell Gradle not to watch files for live changes (Fixes the GitHub Actions crash)
org.gradle.vfs.watch=false

# Tell Gradle not to keep background memory processes running
org.gradle.daemon=false