name: blocked_app
description: Proactive system-wide content moderation.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.10.4      
  image: ^4.1.3                
  fl_chart: ^0.65.0            
  shared_preferences: ^2.2.2   

flutter:
  uses-material-design: true
  assets:
    - assets/models/mobilenet_v3_blocked.tflite
    - assets/models/labels.txt
    
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0